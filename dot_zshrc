# ZSH Default Configs
HISTFILE=~/.histfile
HISTSIZE=9001
SAVEHIST=9001
setopt nomatch
unsetopt autocd beep extendedglob notify
bindkey -e

# Atuin
eval "$(atuin init zsh)"

# Oh-my-posh
eval "$(oh-my-posh init zsh)"
